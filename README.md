# Contextual-Compression-of-Unstructured-Resumes-
RAG-based resume compression using FAISS and LLMs. The pipeline retrieves relevant resume chunks for each schema field and generates validated JSON. Extends the original IEEE ICCRDA 2025 work with field-level F1, ablation studies, token/latency/cost analysis, rule-based baseline, and error analysis.
